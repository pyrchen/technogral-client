import { TButtonSize } from '@/uikit/Button/Button.types';

export const ButtonSizes: Record<TButtonSize, number> = {
	small: 40,
	medium: 50,
	large: 60,
};

export const ButtonFontSizes: Record<TButtonSize, number> = {
	small: 14,
	medium: 16,
	large: 16,
};
