import styled from 'styled-components';

const __TabsWrapper = styled.div`
	display: flex;
	align-items: center;
	justify-content: flex-start;
	gap: 20px;
`;

export const TabsParts = {
	__TabsWrapper,
};
