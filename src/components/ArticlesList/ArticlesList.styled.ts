import styled from 'styled-components';

const __ArticlesList = styled.div`
	display: flex;
	flex-flow: row wrap;
	justify-content: center;
	max-width: 100%;
	column-gap: 20px;
	row-gap: 40px;
`;

export const ArticlesListParts = {
	__ArticlesList,
};
