import styled from 'styled-components';

const __LogoWrapper = styled.div`
	display: inline-flex;
	align-items: center;
	justify-content: center;
	gap: 15px;
`;

export const LogoParts = {
	__LogoWrapper,
};
